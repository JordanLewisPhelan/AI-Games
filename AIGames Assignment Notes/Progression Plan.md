
1. Main Menu - will need this to ensure we have a hub so we can select options to quit, to play versus AI or versus a Player.
      1.1 Game setup should be alterable - We should go from menu to game with AI but with PvP, we must go to a port screen where we connect  to another players lobby or host.
      1.2 Could just have numpad buttons for this, no need to overcomplicate it duh.

2. Game Setup itself - We need to generate 2 Players(regardless of if it is 2 actual players) with a TileMap and a series of pieces, these pieces must all be placed before we can move them. Each turn players will place/move a piece and afterwards the turn ends and active player switches.
   TileMap is the key component here and being able to click on tiles.
3. Add in piece movement logic & visualizers of valid movement.
4. Add on the connection and network system and replace AI with player input - if base game works this should be easy.